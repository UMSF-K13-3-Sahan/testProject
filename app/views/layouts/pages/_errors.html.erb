<% if local_assigns[:obj] !=nil && local_assigns[:obj].errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(local_assigns[:obj].errors.count, "error") %> prohibited
        this article from being saved:
      </h2>
      <ul>
        <% local_assigns[:obj].errors.full_messages.each do |msg| %>
            <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
<% end %>